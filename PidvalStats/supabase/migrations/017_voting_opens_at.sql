-- =====================================================================
-- МІГРАЦІЯ 017 — необов'язковий орієнтовний час відкриття голосування,
-- щоб на сторінці матчу можна було показати таймер зворотного відліку ДО
-- того, як адмін реально натисне "Відкрити голосування" (це й лишається
-- ручною дією, це поле — лише орієнтир для фанатів).
-- =====================================================================

alter table matches add column if not exists voting_opens_at timestamptz;
